import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        backgroundColor: '#c2eb47',
        width: 150,
        borderRadius: 10,
    },
    buttonTitle: {
        fontSize: 22,
        color: "#fff",
        textAlign: "center",
        padding: 12,
        fontWeight: '600'
    }

})